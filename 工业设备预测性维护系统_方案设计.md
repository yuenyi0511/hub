
# 工业设备预测性维护系统 方案设计


## 一、项目概述


### 1.1 项目背景


随着工业4.0与智能制造的深入推进，工业现场设备的运行状态监控与维护策略正经历从“事后维修”向“预测性维护（Predictive Maintenance, PdM）”的深刻转型。然而，当前工业现场普遍面临设备故障滞后性问题：传统基于阈值报警的监控手段往往在故障已经发生甚至造成停机后才触发响应，导致非计划停机时间长、维修成本高、生产连续性受损。此外，工业现场数据呈现多模态、高噪声、样本不平衡（正常样本多，故障样本极少）等特征，传统单一维度的监控难以捕捉设备早期的微弱退化信号。


针对上述痛点，本项目旨在构建一套基于 **Python 3.10** 的 **B/S 三层架构** 工业设备预测性维护系统。该系统深度融合 **OPC UA** 实时数据采集、**Scikit-learn** 传统机器学习及 **PyTorch** 深度学习音频分析技术，通过多模态数据（振动、温度、音频）的协同分析，实现设备健康度评估、故障根因定位及剩余寿命（RUL）预测，并提供基于 **Gradio 6.0** 的可视化交互界面，为工业现场提供低延迟、高可靠、易扩展的智能化运维解决方案。


### 1.2 项目目标


本项目的核心目标是构建一套端到端的预测性维护闭环系统，具体指标如下：


- **实时感知能力**：实现 **1秒级** 的PLC数据轮询与特征提取，端到端延迟控制在 **2.0秒** 以内，确保故障信号的及时捕获。


- **精准诊断能力**：基于 **Isolation Forest** 与专家规则引擎，实现故障分类准确率 **F1 ≥ 0.65**，音频异常检测 **AUC-ROC ≥ 0.95**。


- **寿命预测能力**：建立基于健康度评分的 **RUL（剩余寿命）** 预测模型，支持线性外推与加速衰减两种模式，为备件采购与检修计划提供量化依据。


- **交互可视化**：提供 **8-Tab** 功能的Web交互界面，集成实时仪表盘、音频检测、报警日志及历史趋势分析，降低运维人员使用门槛。


- **高可靠性**：系统支持 **7×24小时** 连续运行，24小时内无崩溃、无内存溢出，内存泄漏率 **< 10MB/天**。


### 1.3 技术方向


本项目坚持 **“边缘计算 + 深度学习 + 工业协议”** 的技术路线：


- **工业互联**：采用 **OPC UA** 作为统一通信架构，通过 **Snap7** 库与 **TIA Portal** 仿真环境对接，确保与真实工业现场PLC（S7-1200/1500）的无缝兼容。


- **多模态融合**：结合 **Scikit-learn** 处理结构化时序数据（振动、温度）与 **PyTorch** 处理非结构化音频数据（Mel频谱），利用 **ResNet18 + Memory Bank KNN** 解决工业音频小样本异常检测难题。


- **轻量化部署**：选用 **SQLite** 作为本地时序数据库，**Gradio 6.0** 作为Web框架，**CSV** 作为轻量级持久化存储，避免对重型工业数据库或云平台的依赖，实现单机私有化部署。


### 1.4 术语与缩写


| 术语/缩写 | 全称 | 说明 |
| --- | --- | --- |
| PdM | Predictive Maintenance | 预测性维护，基于设备状态监测数据预测故障发生时间 |
| RUL | Remaining Useful Life | 剩余使用寿命，设备在失效前还能正常运行的时间 |
| OPC UA | Open Platform Communications Unified Architecture | 开放平台通信统一架构，工业通信国际标准 |
| PLC | Programmable Logic Controller | 可编程逻辑控制器，工业现场核心控制单元 |
| FFT | Fast Fourier Transform | 快速傅里叶变换，用于振动/音频信号的频域分析 |
| RMS | Root Mean Square | 均方根值，衡量振动能量或信号强度的关键指标 |
| KNN | K-Nearest Neighbors | K近邻算法，用于音频特征空间的异常判定 |
| B/S | Browser/Server | 浏览器/服务器架构，本系统的前后端交互模式 |


## 二、需求分析


### 2.1 功能需求分析


#### 2.1.1 功能模块总览


系统共规划 **9个核心功能模块**，按业务逻辑与优先级排序如下：


| 优先级 | 模块编号 | 模块名称 | 源文件 | 核心功能描述 |
| --- | --- | --- | --- | --- |
| P0 | M-01 | PLC数据采集 | data_acquisition.py | 连接博图PLC，1s轮询12个变量，CSV持久化，断线重连 |
| P0 | M-03 | 故障诊断 | fault_diagnosis.py | 孤立森林异常检测、健康度评分、规则故障分类、RUL预测 |
| P0 | M-02 | 特征工程 | feature_engineering.py | 滑动窗口(30/10)，时域/温度/状态特征提取，共53维 |
| P1 | M-04 | 音频推理 | predict.py | 命令行单条音频推理，支持5类设备，动态阈值加载 |
| P1 | M-06 | 实时仪表盘 | visualize.py | 振动/温度趋势、RMS-退化双轴图、FFT频谱分析 |
| P1 | M-07 | Web交互(MIMII) | gradio_app.py | 8-Tab界面，音频检测、PLC监控、报警日志集成 |
| P2 | M-05 | 评估可视化 | visualization_all.py | ROC/混淆矩阵/t-SNE/异常分数分布，F1阈值网格搜索 |
| P2 | M-08 | Web交互(完整版) | web_app.py | 全功能集成，包含高级分析与历史对比 |
| P3 | M-09 | 流水线调度 | run_pipeline.py | 6种运行模式入口，全流程编排 |


#### 2.1.2 非功能性需求


- **实时性要求**：


- 端到端延迟（从PLC数据产生到Web界面刷新）：**≤ 2.0s**（实测1.2s）。


- 诊断响应时间（从特征输入到结果输出）：**≤ 500ms**（实测320ms）。


- 音频推理耗时（单条16kHz WAV）：**≤ 1.0s**（实测450ms）。


- 图表渲染时间：**≤ 1.0s**（实测680ms）。


- **准确性要求**：


- 音频异常检测 **AUC-ROC ≥ 0.95**。


- 故障分类 **F1-Score ≥ 0.65**（在工业现场样本极度不平衡下）。


- **可靠性要求**：


- 支持 **7×24小时** 无人值守连续运行。


- 24小时运行无崩溃、无内存溢出。


- 内存泄漏率 **< 10MB/天**（实测6.2MB/天）。


- 具备PLC断线自动重连与异常恢复机制。


- **可扩展性要求**：


- B/S架构，支持新增传感器变量或设备类型的热插拔配置。


- 算法模型支持独立更新，无需重启Web服务。


### 2.2 约束与假设


- **硬件约束**：部署环境需配备 **Intel i9-14700H + RTX 4060** 或同等算力硬件，支持 **CUDA** 加速；PLC服务器与PLC需处于同一局域网，**OPC UA 4840端口** 开放。


- **软件约束**：操作系统为 **Windows 10/11** 或 **Windows Server 2019+**；PLC仿真环境为 **PLCSIM Advanced**，控制软件为 **TIA Portal V16+**。


- **数据假设**：假设PLC采集的12个变量（三轴振动、温度、转速等）已经过底层滤波处理，无严重丢包；MIMII音频数据集采样率统一为 **16kHz**。


- **网络假设**：局域网内OPC UA通信延迟 **< 10ms**，带宽 **> 100Mbps**。


## 三、总体方案设计


### 3.1 方案论证


#### 3.1.1 总体方案对比


针对工业预测性维护系统的落地，我们对比了三种主流技术路线：


| 对比维度 | 方案A：PLC+Python全栈 | 方案B：Matlab+Simulink | 方案C：Node-RED+云平台 |
| --- | --- | --- | --- |
| 数据采集 | 原生支持OPC UA，与工业现场一致 | 需额外配置OPC客户端，配置繁琐 | 依赖云端IoT网关，延迟较高，受网络影响大 |
| 深度学习 | PyTorch生态完善，部署灵活，社区活跃 | 深度学习工具箱受限，模型导出困难 | 需调用外部API，定制性差，数据隐私风险高 |
| 前后端集成 | Gradio，轻量高效，Python原生支持 | 需单独开发Web界面，前后端分离复杂 | 依赖云平台UI，本地化弱，离线不可用 |
| 部署成本 | 开源软件为主，零授权费，成本低 | 商业授权费用高昂，按节点/核心计费 | 云服务按量计费，长期运营成本高 |
| 工程落地 | 贴近真实工业架构，可迁移性强 | 偏向学术仿真，落地困难，环境依赖重 | 强依赖网络，离线不可用，数据主权弱 |


#### 3.1.2 选型结论


**选定方案A（PLC + Python全栈）** 作为最终实施方案。


**理由**： 1. **技术自主性**：全栈Python技术栈，无商业软件授权限制，便于二次开发与长期维护。 2. **工业兼容性**：直接对接OPC UA与TIA Portal，无需中间件转换，数据链路最短。 3. **AI落地能力**：PyTorch + Gradio组合，完美解决“算法模型”到“Web应用”的最后一公里，支持实时交互与可视化。 4. **成本效益**：SQLite + CSV + 开源库，单机部署成本趋近于零，适合中小产线或边缘侧部署。


### 3.2 系统架构设计


#### 3.2.1 总体架构（五层架构说明）


系统采用 **B/S 三层架构** 与 **五层逻辑分层** 相结合的设计：


- **控制层（Control Layer）**：


- 核心：TIA Portal + S7-1200/1500 PLC（或PLCSIM Advanced）。


- 职责：逻辑控制、传感器信号采集、OPC UA服务端发布。


- **通信层（Communication Layer）**：


- 核心：OPC UA协议 + Snap7客户端。


- 职责：跨平台数据交换、安全认证、1秒级同步轮询。


- **数据层（Data Layer）**：


- 核心：SQLite（元数据/报警） + CSV（时序原始数据）。


- 职责：轻量级持久化、历史数据回溯、断点续传。


- **算法层（Algorithm Layer）**：


- 核心：PyTorch（音频/深度学习） + Scikit-learn（时序/异常检测）。


- 职责：特征工程、Isolation Forest异常检测、ResNet18音频推理、RUL预测。


- **服务层（Service Layer）**：


- 核心：Gradio 6.0 + Matplotlib + FastAPI（底层）。


- 职责：Web交互、实时仪表盘、8-Tab功能集成、API暴露。


#### 3.2.2 技术栈总览


| 层级 | 技术组件 | 版本/规格 | 选型理由 |
| --- | --- | --- | --- |
| 语言 | Python | 3.10 | 兼顾性能与生态，Type Hint支持好 |
| 通信 | OPC UA / Snap7 | Latest | 工业标准，兼容S7-1200/1500 |
| 仿真 | PLCSIM Advanced | V3.0+ | 完美替代物理PLC，支持OPC UA |
| ML框架 | Scikit-learn | 1.3+ | Isolation Forest，工业异常检测首选 |
| DL框架 | PyTorch | 2.0+ | 动态图，ResNet18预训练，CUDA加速 |
| 音频处理 | Librosa | 0.10+ | Mel频谱提取，工业音频特征标准库 |
| Web框架 | Gradio | 6.0 | 极速构建AI Demo，支持实时刷新 |
| 数据库 | SQLite | 3.40+ | 零配置，单文件，适合边缘部署 |
| 可视化 | Matplotlib | 3.7+ | 科学绘图标准，支持FFT/频谱图 |


### 3.3 数据流设计


系统数据流遵循 **“采集 → 存储 → 特征 → 诊断 → 展示”** 的单向管道设计：


- **PLC生成传感器数据** → **OPC UA客户端**（data_acquisition.py）采集 → 保存为 **CSV文件**（plc_data_YYYYMMDD_HHMMSS.csv）。


- **特征工程模块**（feature_engineering.py）读取CSV → **滑动窗口分割**（30/10） → **时频域特征提取**（53维） → 输出特征数据集。


- **故障诊断模块**（fault_diagnosis.py）读取特征数据 → **Isolation Forest异常检测** + **规则引擎分类** + **RUL预测** → 输出诊断报告/报警。


- **音频检测模块**（predict.py）读取MIMII数据集/WAV文件 → **ResNet18提取512维特征** → **Memory Bank KNN判定** → 输出异常分数/标签。


- **可视化模块**（visualize.py / gradio_app.py）生成仪表盘与评估图表 → **Gradio Web界面** 实时展示（1s刷新）。


## 四、功能模块设计


### 4.1 PLC数据采集模块 (data_acquisition.py)


**核心职责**：建立与PLC的稳定连接，实现高频、低延迟的数据轮询与持久化。


- **连接配置**：


- OPC UA地址：opc.tcp://192.168.0.1:4840


- 命名空间：ns=3


- 采集周期：**1000ms**（同步轮询，非事件触发，保证时序连续性）。


- **采集变量**：共 **12个** 核心变量：


- 三轴振动：vibration_x, vibration_y, vibration_z


- 振动能量：rms_value


- 温度监测：bearing_temp, motor_temp, ambient_temp


- 运行状态：run_time, rpm, load, degradation, fault_type


- **持久化策略**：


- 文件命名：plc_data_YYYYMMDD_HHMMSS.csv


- 写入方式：Append模式，避免频繁IO。


- 异常处理：PLC断线自动重连（指数退避策略），数据缓存至内存队列，恢复后补写。


### 4.2 特征工程模块 (feature_engineering.py)


**核心职责**：将原始时序数据转化为高维、高信息量的特征向量。


- **窗口策略**：


- 窗口大小：**30个采样点**（覆盖30秒数据，捕捉短期趋势）。


- 滑动步长：**10个采样点**（30%重叠，平衡计算量与特征连续性）。


- **特征维度（共53维）**：


- **时域统计（30维）**：均值、方差、偏度、峰度、RMS、峰值因子、裕度因子等（每轴10个）。


- **温度特征（12维）**：三轴温度均值、方差、梯度、变化率等。


- **RMS综合（4维）**：RMS趋势、RMS波动、RMS峰值、RMS基线偏移。


- **运行状态（5维）**：转速归一化、负载率、运行时长归一化、退化度、故障码One-hot。


- **温度梯度（2维）**：轴承-环境温差梯度、电机-轴承温差梯度。


### 4.3 故障诊断模块 (fault_diagnosis.py)


**核心职责**：融合无监督异常检测与专家规则，实现“异常发现 + 根因定位 + 寿命预测”。


- **异常检测**：


- 算法：**Isolation Forest**


- 参数：contamination=0.1, n_estimators=200, n_features=12


- 输出：异常分数（Anomaly Score），分数越低越异常。


- **规则分类**：基于物理机理的专家系统（详见5.2节）。


- **RUL预测**：基于健康度评分的分段预测模型（详见5.3节）。


### 4.4 音频异常检测模块 (predict.py)


**核心职责**：利用深度学习处理非结构化音频，解决振动传感器盲区问题。


- **模型架构**：**ResNet18**（ImageNet预训练） + **Memory Bank KNN**。


- **输入处理**：


- 采样率：16kHz


- 特征：Mel频谱图（n_mels=64, n_fft=1024, hop_length=512）


- 输出：512维特征向量


- **支持设备**：Fan, Bearing, Gearbox, Slider, Valve（5类，MIMII数据集，18000+ WAV）。


- **推理模式**：命令行单条推理，支持动态阈值加载（根据设备类型自动切换）。


### 4.5 可视化与Web交互模块


**核心职责**：将复杂算法结果转化为直观的运维决策依据。


- **实时仪表盘 (visualize.py)**：


- 振动/温度趋势图（双Y轴）。


- RMS-退化度关联图（预测失效点）。


- FFT频谱分析（识别故障特征频率）。


- **Web交互 (gradio_app.py / web_app.py)**：


- 框架：**Gradio 6.0**


- 布局：**8个Tab页面**（音频检测、PLC监控、报警日志、历史趋势、模型评估、系统配置、RUL预测、关于）。


- 交互：WAV上传（限制50MB）、设备类型下拉、实时刷新（every=1s）。


- **评估可视化 (visualization_all.py)**：


- ROC曲线、混淆矩阵、t-SNE特征分布、异常分数直方图。


- F1阈值网格搜索热力图。


### 4.6 统一流水线调度模块 (run_pipeline.py)


**核心职责**：提供标准化的运行入口，解耦模块依赖。


- **6种运行模式**：


- collect：PLC采集（默认60s测试）。


- diagnose：读取最新CSV执行故障诊断。


- detect：单条音频推理。


- visualize：生成MIMII评估图表。


- web：启动Gradio服务（Port 7860）。


- full：采集 → 诊断 → 可视化全流程（生产模式）。


## 五、关键技术设计


### 5.1 异常检测算法设计


**算法选择**：**Isolation Forest (孤立森林)**


**设计理由**： 1. **无监督学习**：工业现场故障样本极度稀缺，Isolation Forest无需故障标签，仅利用正常数据建模。 2. **高维适应性**：对53维特征向量处理高效，不受“维度灾难”影响。 3. **计算效率**：时间复杂度 O(NlogN)，满足1秒级实时诊断需求。


**参数配置**： - contamination=0.1：预设10%异常比例，适应工业现场初期数据清洗阶段。 - n_estimators=200：200棵孤立树，平衡稳定性与计算开销。 - max_samples='auto'：自动采样，适应不同窗口大小。


### 5.2 故障分类规则设计


采用 **“数据驱动 + 知识驱动”** 双引擎，当Isolation Forest触发异常后，由规则引擎进行根因定位：


| 触发条件 | 故障类型 | 物理机理 |
| --- | --- | --- |
| vibration_x > 4.5 | 轴承磨损（径向水平） | 水平方向冲击载荷增大 |
| vibration_y > 4.5 | 轴承磨损（径向垂直） | 垂直方向支撑刚度下降 |
| `vibration_z |  |  |
