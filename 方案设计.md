## 二、方案设计

### 2.1 功能需求分析

#### 2.1.1 功能模块需求

系统需实现从底层数据采集到上层可视化展示的完整功能闭环，具体功能模块及优先级划分如下：

| 功能模块 | 功能描述 | 优先级 |
|----------|----------|--------|
| 数据采集 | 通过 OPC UA 从 PLC 实时采集振动、温度等多维传感器数据 | P0（必须） |
| 数据存储 | 高频时序数据存入 InfluxDB，设备台账与报警记录存入 MySQL | P0（必须） |
| 故障分类 | 基于 CNN 模型对轴承振动信号频谱图进行分类，识别正常/内圈/外圈故障 | P0（必须） |
| RUL 预测 | 基于 LSTM/GRU 模型预测设备剩余使用寿命，输出预测曲线 | P0（必须） |
| 异常检测 | 基于 Autoencoder 无监督学习，在故障样本极少时识别早期异常 | P1（重要） |
| 实时推理 | 后端服务加载训练好的模型，对实时流入数据进行在线推理预测 | P0（必须） |
| 可视化监控 | Web 前端展示实时参数曲线、故障报警弹窗、RUL 预测趋势图 | P0（必须） |
| 报警反馈 | 判定故障后通过 OPC UA 写回 PLC 触发报警标志位，形成闭环 | P1（重要） |
| 模型部署优化 | 模型剪枝与量化，降低推理延迟至 100ms 以内 | P2（可选） |

#### 2.1.2 非功能性需求

- **实时性**：系统需具备高并发处理能力，单次模型推理延迟必须控制在 ≤ 100ms，以保证工业现场的控制时效。
- **准确性**：故障分类准确率需达到 ≥ 95%，RUL 预测误差需控制在 ≤ 10%，以满足实际运维决策需求。
- **可靠性**：系统需具备高可用性，要求连续稳定运行 72 小时以上无崩溃、无内存泄漏。
- **可扩展性**：架构设计需支持多设备并行接入与多模型同时推理，便于后续扩展至更多工业场景。

### 2.2 方案论证

#### 2.2.1 总体方案对比

针对工业预测性维护系统的构建，目前主流存在三种技术路线，具体对比如下：

| 对比维度 | 方案A：博图PLC+Python全栈 | 方案B：Matlab+Simulink | 方案C：Node-RED+云平台 |
|----------|---------------------------|------------------------|------------------------|
| 数据采集 | OPC UA 直连 PLC，灵活度高 | Simulink 仿真集成好，但工业部署弱 | 依赖云平台，延迟较高 |
| 深度学习 | PyTorch/TF 生态成熟，模型丰富 | Deep Learning Toolbox 功能全但封闭 | 依赖第三方 API，定制性差 |
| 前后端 | Vue+FastAPI 全栈自主可控 | 内置报表功能，Web 能力弱 | 低代码快速搭建，但复杂逻辑受限 |
| 部署成本 | 开源免费，边缘部署灵活 | 商业授权成本高 | 云服务费用不可控 |
| 工程落地 | Git+Docker 标准化工程流 | 版本管理与协作能力弱 | 平台锁定，迁移困难 |

**论证结论**：综合对比，方案 A（博图 PLC + Python 全栈）在数据采集灵活性、深度学习生态、前后端自主可控性及工程化落地能力上均显著优于其他方案，且完全开源免费，高度契合本课题的学术研究与工程实践目标，因此选定方案 A。

#### 2.2.2 关键技术选型论证

| 技术选型 | 候选方案 | 选定方案 | 选择理由 |
|----------|----------|----------|----------|
| PLC 仿真 | PLCSIM / PLCSIM Advanced | PLCSIM Advanced | 支持 OPC UA 服务器，可与外部 Python 程序无缝通信 |
| 深度学习框架 | PyTorch / TensorFlow | PyTorch | 动态计算图更利于实验调试，社区生态活跃，学术资源丰富 |
| 后端框架 | FastAPI / Flask | FastAPI | 原生异步支持，自动生成交互式 API 文档，推理延迟更低 |
| 时序数据库 | InfluxDB / TimescaleDB | InfluxDB | 专为时序数据设计，写入与查询性能优异，资源占用低 |
| 前端框架 | Vue.js / React | Vue.js + ECharts | ECharts 工业图表组件丰富，Vue 学习曲线平缓，开发效率高 |
| AI 辅助编程 | Kimi Code / 通义灵码 / Qwen Code | Kimi Code（主力）+ 通义灵码（备选） | Kimi Code 为 VSCode 插件，中文交流顺畅，长上下文适合项目级代码理解 |

### 2.3 方案规划

#### 2.3.1 技术栈总览

| 层次 | 技术组件 | 版本/说明 |
|------|----------|-----------|
| 控制层 | TIA Portal V17 + PLCSIM Advanced | 博图仿真环境，S7-1200/1500 PLC |
| 通信层 | OPC UA（asyncua 库） | PLC ↔ Python 数据桥梁 |
| 数据层 | InfluxDB 2.x + MySQL 8.0 | 时序数据 + 关系型数据 |
| 算法层 | PyTorch 2.x + NumPy + Pandas | 深度学习模型训练与推理 |
| 服务层 | FastAPI + Uvicorn | 异步 RESTful API 与推理服务 |
| 前端层 | Vue 3 + ECharts 5 | B/S 架构监控大屏 |
| 工程化 | Git + Docker + Kimi Code | 版本管理、容器化部署、AI 辅助编程 |

#### 2.3.2 外部资源与数据集规划

**外部资源配置：**

| 资源类型 | 具体说明 | 获取方式 |
|----------|----------|----------|
| PLC 仿真软件 | TIA Portal V17（含 PLCSIM Advanced） | 西门子官方教育版或校园授权 |
| 数据库服务 | InfluxDB 2.x + MySQL 8.0 | 官方 Docker 镜像一键部署 |
| Python 环境 | Python 3.10+，PyTorch GPU/CPU | PyPI / Conda 安装 |
| 前端开发工具 | Node.js 18+，VS Code | 官方下载安装 |
| AI 辅助编程 | Kimi Code（VS Code 插件） | VS Code 扩展市场安装 |
| 版本管理 | Git + GitHub 私有仓库 | Git SCM + GitHub 账号 |

**数据集来源：**

| 数据集名称 | 内容描述 | 用途 | 获取方式 |
|------------|----------|------|----------|
| CWRU 轴承数据集 | 凯斯西储大学轴承故障数据，含正常/内圈/外圈/滚动体故障的振动信号 | CNN 故障分类模型训练与验证 | 凯斯西储大学官网公开下载 |
| NASA C-MAPSS 数据集 | 航空发动机剩余使用寿命（RUL）仿真数据，含多传感器时序记录 | LSTM/GRU 寿命预测模型训练 | NASA 官网公开下载 |
| 仿真数据（自生成） | 基于 PLCSIM Advanced 模拟设备运行，写入噪声与趋势项的振动/温度数据 | 实时推理服务测试与闭环验证 | 博图仿真环境自行生成 |

#### 2.3.3 前端 UI 设计方案

系统前端采用 B/S 架构，基于 Vue 3 单页面应用（SPA）开发，通过 Axios 与 FastAPI 后端进行 RESTful API 通信。页面布局规划如下：

- **顶部导航栏**：展示系统名称、当前时间、设备连接状态。
- **左侧面板**：设备列表展示、设备选型切换。
- **主展示区**：实时参数曲线（振动加速度、温度）采用 ECharts 折线图，1 秒刷新间隔；故障分类结果通过仪表盘与状态指示灯（正常/内圈故障/外圈故障/滚动体故障）展示；RUL 预测趋势图展示预测值与置信区间；异常检测得分通过实时滚动热力图呈现。
- **底部告警栏**：故障报警弹窗，显示报警时间、设备、故障类型及建议措施。
- **交互设计**：故障报警时触发弹窗与声音提示；支持历史数据时间范围查询与导出。

#### 2.3.4 后端算法方案

**数据预处理流水线**：原始传感器数据 → 3σ 去噪 → 插值填充缺失值 → 滑动窗口切片 → Min-Max 归一化 → 模型输入。其中滑动窗口长度设为 50 步，步长 25 步；对振动信号进行 FFT 变换获取频谱图作为 CNN 输入。

**核心模型架构：**

| 模型 | 架构 | 输入 | 输出 | 损失函数 |
|------|------|------|------|----------|
| CNN（故障分类） | 2D 卷积层(3层) + 全局池化 + 全连接层 | 频谱图 (64×64) | 故障类别概率分布 | CrossEntropyLoss |
| LSTM（RUL预测） | LSTM层(2层, 128单元) + Dropout + 全连接层 | 多变量时序 (50步×6维) | RUL 连续值 | MSELoss |
| Autoencoder（异常检测） | 编码器(2层) + 解码器(2层) | 归一化时序片段 | 重构误差标量 | MSELoss |

**推理服务设计**：FastAPI 提供 /predict/classify、/predict/rul、/predict/anomaly 三个推理接口，模型采用 ONNX Runtime 加速推理，目标延迟 < 100ms，支持批量推理与流式推理两种模式。

#### 2.3.5 系统数据流

系统整体数据流转路径为：[PLC] –OPC UA→ [InfluxDB] –读取→ [FastAPI后端] → [PyTorch模型推理] → [故障分类/RUL预测/异常检测] → [MySQL记录] 和 [Vue前端ECharts] → [报警标志位] ←OPC UA– [写回PLC触发报警]。

### 2.4 技术路线

#### 2.4.1 总体技术路线

本课题采用五层递进式技术路线：

1. **第一层（数据层）**：搭建 PLC 仿真环境，采集传感器数据并存入时序数据库。
2. **第二层（算法层）**：执行数据预处理，完成模型训练、调优、评估与 ONNX 导出。
3. **第三层（服务层）**：搭建 FastAPI 推理服务，集成 OPC UA 客户端，实现实时数据订阅与推理。
4. **第四层（展示层）**：开发 Vue 前端监控大屏，对接后端 API，实现可视化与报警功能。
5. **第五层（工程化）**：进行模型轻量化处理，探索边缘部署，完成系统测试与优化。

#### 2.4.2 AI 辅助开发策略

本课题全面引入 AI 辅助编程（Vibe Coding）策略：

- **PLC 代码生成**：使用 Kimi Code 生成 S7-1200 的 OPC UA 数据读取块（SCL 代码）。
- **算法代码生成**：通过明确输入输出规范，利用 AI 生成模型训练与评估脚本。
- **前后端代码生成**：利用 AI 生成 FastAPI 接口模板与 Vue 组件骨架，人工负责核心逻辑审查与修改，大幅提升开发效率。

### 2.5 参考文献

[1] Siemens AG. TIA Portal V17 Documentation[Z]. 2023.

[2] OPC Foundation. OPC Unified Architecture Specification. Part 1-10[S]. 2023.

[3] LeCun Y, Bottou L, Bengio Y, et al. Gradient-based learning applied to document recognition[J]. Proceedings of the IEEE, 1998, 86(11): 2278-2324.

[4] Hochreiter S, Schmidhuber J. Long short-term memory[J]. Neural Computation, 1997, 9(8): 1735-1780.

[5] Vincent P, Larochelle H, Lajoie I, et al. Stacked denoising autoencoders: Learning with noisy inputs[C]//Advances in Neural Information Processing Systems. 2010, 23.

[6] Randall V B, Antoni J. Rolling element bearing diagnostics — A tutorial[J]. Mechanical Systems and Signal Processing, 2011, 25(2): 485-520.

[7] Saxena J, Goebel K, Simon D, et al. Damage propagation modeling for aircraft engine run-to-failure simulation[C]//2008 International Conference on Prognostics and Health Management. IEEE, 2008.

[8] PyTorch Team. PyTorch Documentation[EB/OL]. https://pytorch.org/docs/. 2024.

[9] Tiangolo S. FastAPI Documentation[EB/OL]. https://fastapi.tiangolo.com/. 2024.

[10] 张宏林. 基于深度学习的旋转机械故障诊断方法研究[J]. 机械工程学报, 2022, 58(15): 1-12.
