# 工业设备预测性维护系统 软件规格说明书

## 一、引言

### 1.1 编写目的

本《软件规格说明书》旨在明确"工业设备预测性维护系统 v4.2"的功能需求、非功能需求、接口规范及运行环境。文档作为系统设计、开发、测试及验收的唯一技术依据，确保开发团队与需求方对系统边界、数据流转及核心算法逻辑达成一致。

### 1.2 项目背景

针对工业现场设备故障滞后性问题，本系统基于 Python 3.10 构建 B/S 三层架构，融合 OPC UA 实时数据采集、Scikit-learn 传统机器学习及 PyTorch 深度学习音频分析技术。系统通过多模态数据（振动、温度、音频）实现设备健康度评估、故障根因定位及剩余寿命（RUL）预测，并提供 Gradio 6.0 可视化交互界面。

### 1.3 术语与缩写

| 术语/缩写 | 全称 | 说明 |
|-----------|------|------|
| OPC UA | Open Platform Communications Unified Architecture | 工业通信标准，用于PLC数据交互 |
| PLC | Programmable Logic Controller | 可编程逻辑控制器 |
| RUL | Remaining Useful Life | 剩余使用寿命 |
| MIMII | Malfunctioning Industrial Machine Investigation and Inspection | 工业机器异常声音数据集 |
| RMS | Root Mean Square | 均方根值，振动能量指标 |
| FFT | Fast Fourier Transform | 快速傅里叶变换 |
| t-SNE | t-Distributed Stochastic Neighbor Embedding | t-分布随机邻域嵌入，降维可视化 |
| B/S | Browser/Server | 浏览器/服务器架构 |

### 1.4 参考资料

- IEC 62541: OPC UA 通信协议标准
- MIMII Dataset: Zenodo Archive (5类设备，16kHz采样)
- Scikit-learn Documentation: Isolation Forest & Linear Regression
- Gradio 6.0 Official Documentation

---

## 二、系统概述

### 2.1 系统描述

本系统采用模块化设计，核心逻辑封装于 `src/` 目录下的 9 个 Python 源文件中。系统支持从 PLC 实时采集 12 维传感器数据，经滑动窗口特征工程后，执行孤立森林异常检测与规则专家系统故障诊断；同时支持 MIMII 音频文件的离线/在线异常推理。所有功能通过 `run_pipeline.py` 统一调度，并通过 Gradio Web 界面呈现。

### 2.2 系统边界

- **包含**：PLC 数据采集、特征提取、统计/规则故障诊断、音频推理、可视化图表生成、Web 交互服务、流水线调度。
- **不包含**：PLC 内部逻辑控制、设备物理维修执行、非 MIMII 协议音频的预处理、企业级 ERP/MES 集成。

### 2.3 用户特征

- **运维工程师**：使用 Web 仪表盘监控实时状态，查看报警日志。
- **算法工程师**：使用 `predict.py` 或 `visualize_all.py` 进行模型评估与阈值调优。
- **系统管理员**：配置 `run_pipeline.py` 运行模式，管理 SQLite 数据库与文件存储。

### 2.4 假设与约束

- **硬件假设**：运行环境具备 Intel i7-12700H + RTX 4060 或同等算力，支持 CUDA 加速。
- **网络假设**：应用服务器与 PLC（192.168.0.1）处于同一局域网，OPC UA 端口 4840 开放。
- **数据约束**：PLC 数据块命名空间固定为 ns=3，节点 ID 必须包含完整 DB 名称。
- **音频约束**：输入音频必须为 WAV 格式，采样率 16kHz，单声道。

---

## 三、功能规格

### 3.1 功能模块总览

| 模块编号 | 模块名称 | 源文件 | 核心功能描述 |
|----------|----------|--------|--------------|
| M-01 | PLC数据采集 | data_acquisition.py | 连接博图PLC，1s轮询12个变量，CSV持久化，断线重连 |
| M-02 | 特征工程 | feature_engineering.py | 滑动窗口(30/10)，时域/温度/状态特征提取，共40+维 |
| M-03 | 故障诊断 | fault_diagnosis.py | 孤立森林异常检测、健康度评分、规则故障分类、RUL预测 |
| M-04 | 音频推理 | predict.py | 命令行单条音频推理，支持5类设备，动态阈值加载 |
| M-05 | 评估可视化 | visualization_all.py | ROC/混淆矩阵/t-SNE/异常分数分布，F1阈值网格搜索 |
| M-06 | 实时仪表盘 | visualize.py | 振动/温度趋势、RMS-退化双轴图、FFT频谱分析 |
| M-07 | Web交互(MIMII) | gradio_app.py | 8-Tab界面，音频检测、PLC监控、报警日志集成 |
| M-08 | Web交互(完整版) | web_app.py | 全功能集成，包含高级分析与历史对比 |
| M-09 | 流水线调度 | run_pipeline.py | 6种运行模式入口，全流程编排 |

### 3.2 数据采集功能规格

#### 3.2.1 输入与配置

- OPC UA 地址：`opc.tcp://192.168.0.1:4840`
- 命名空间：ns=3
- 采集周期：1000ms（同步轮询）
- 目标变量：

| 数据块 (DB) | 变量名 | 物理含义 | 数据类型 |
|-------------|--------|----------|----------|
| DB10_Vibration | vibration_x, vibration_y, vibration_z | 三轴振动加速度 | Float |
| DB10_Vibration | rms_value | 振动均方根值 | Float |
| DB20_Temperature | bearing_temp, motor_temp, ambient_temp | 轴承/电机/环境温度 | Float |
| DB30_Status | runtime_hours, rpm, load_percent | 运行时间/转速/负载 | Int/Float |
| DB30_Status | degradation_level, fault_type | 退化等级/故障类型 | Int/String |

#### 3.2.2 处理逻辑

1. 初始化 OPC UA Client，建立安全会话。
2. 循环读取 12 个节点值，异常时触发断线重连机制。
3. 将时间戳与数据打包，追加写入 CSV 文件。
4. 文件命名规范：`plc_data_YYYYMMDD_HHMMSS.csv`。

#### 3.2.3 异常处理

- **连接失败**：记录日志，等待 5s 后重试，连续 3 次失败抛出 ConnectionError。
- **节点不存在**：跳过该变量，记录 Warning，不中断采集循环。

### 3.3 特征工程功能规格

#### 3.3.1 窗口策略

- 窗口大小：30 个采样点
- 滑动步长：10 个采样点
- 填充策略：不足窗口大小时不计算，丢弃尾部数据

#### 3.3.2 特征提取清单

| 特征类别 | 具体特征 | 计算方法/来源 | 数量 |
|----------|----------|---------------|------|
| 时域统计 | 均值, 标准差, 最大值, 最小值, 峰峰值, RMS, 峭度, 偏度, 波形因子, 峰值因子 | 三轴振动分别计算 | 30 |
| 温度特征 | 均值, 最大值, 趋势斜率, 标准差 | 线性拟合斜率，三温度源 | 12 |
| 温度梯度 | 轴承-环境差, 电机-环境差 | 差值计算 | 2 |
| RMS综合 | 均值, 标准差, 最大值, 峰值因子 | 基于 rms_value 窗口统计 | 4 |
| 运行状态 | 运行时间, 转速, 负载, 退化度, 故障类型 | 直接透传窗口内最新值 | 5 |

### 3.4 异常检测功能规格

- **算法模型**：Isolation Forest
- **关键参数**：
  - contamination: 0.1
  - n_estimators: 200
  - n_features: 12 (仅输入核心传感器特征)
- **输出**：
  - anomaly_score: 异常得分（负值越大越异常）
  - health_label: 0 (正常) / 1 (异常)

### 3.5 故障分类功能规格

采用规则专家系统，优先级从上至下匹配：

| 触发条件 | 故障诊断结论 | 备注 |
|----------|--------------|------|
| vibration_x > 4.5 | 轴承磨损（振动X轴超标） | 径向水平振动异常 |
| vibration_y > 4.5 | 轴承磨损（振动Y轴超标） | 径向垂直振动异常 |
| vibration_z > 4.5 | 轴向不对中（振动Z轴超标） | 轴向振动异常 |
| kurtosis > 5.0 | 滚动体局部缺陷（峭度异常） | 冲击性故障 |
| bearing_temp > 70℃ | 轴承过热 | 绝对温度阈值 |
| motor_temp > 80℃ | 电机过热 | 绝对温度阈值 |
| temp_gradient > 20 | 散热不良/润滑失效 | 温差过大 |
| vibration_x > 3.0 | 振动X轴偏高，疑似早期磨损 | 预警阈值 |
| kurtosis > 3.0 | 峭度升高，疑似滚动体点蚀 | 预警阈值 |
| bearing_temp_trend > 0.01 | 轴承温度持续上升，疑似润滑劣化 | 趋势阈值 |

### 3.6 剩余寿命预测功能规格

基于 degradation_level 反推 RUL（剩余使用寿命）：

| 健康度区间 | 衰减模型 | 基准点/逻辑 |
|------------|----------|-------------|
| ≥ 80 | 线性外推 | 100分 = 1250小时，线性递减 |
| 50 - 80 | 加速衰减 | 非线性曲线，寿命缩短速率增加 |
| 20 - 50 | 急剧缩短 | 指数级衰减，需立即干预 |
| < 20 | 趋近于 0 | imminent failure，RUL ≈ 0 |

### 3.7 音频异常检测功能规格

- **入口**：`python src/predict.py <音频路径> --device <设备名>`
- **支持设备**：fan, bearing, gearbox, slider, valve
- **阈值管理**：
  - 默认加载：`data/models/mimii_final_report.json`
  - 覆盖参数：`--threshold <float>`
- **输出**：JSON 格式结果，包含 is_anomaly, score, threshold_used。

### 3.8 可视化功能规格

#### 3.8.1 模型评估可视化 (visualization_all.py)

- ROC 曲线：5 类设备 AUC 对比。
- 混淆矩阵：热力图展示分类准确率。
- t-SNE 散点图：特征空间分布，验证聚类效果。
- 异常分数直方图：正/负样本分数分布。
- 阈值搜索：验证集网格搜索 100 个点，寻找最优 F1-Score 阈值。

#### 3.8.2 实时仪表盘 (visualize.py)

- 振动趋势：三轴曲线 + 4.5mm/s 报警红线。
- 温度趋势：三温曲线 + 70℃ 报警红线。
- 健康趋势：RMS (左Y) 与 退化程度 (右Y) 双轴图。
- 频谱分析：FFT 幅频特性图。

### 3.9 Web交互界面功能规格

基于 Gradio 6.0 构建，包含 8 个 Tab：

1. **MIMII音频检测**：WAV 上传 + 设备选择 + 结果展示。
2. **PLC数据采集**：启动/停止采集，实时数据表格。
3. **故障诊断**：当前状态卡片 + 故障规则命中列表。
4. **仪表盘**：嵌入 visualize.py 生成的实时图表。
5. **高级分析**：t-SNE、ROC、阈值分布图。
6. **实时监控**：滚动日志流。
7. **历史对比**：CSV 数据趋势回放。
8. **音频检测**：批量/单条推理入口。

> 注：集成报警日志记录功能，自动写入 docs/ 目录。

### 3.10 统一流水线功能规格

**入口**：`run_pipeline.py`

| 模式 | 命令示例 | 功能描述 |
|------|----------|----------|
| collect | python run_pipeline.py collect --duration 60 | PLC 采集，默认 60s |
| diagnose | python run_pipeline.py diagnose | 读取最新 CSV 执行故障诊断 |
| detect | python run_pipeline.py detect <wav> <dev> | 单条音频推理 |
| visualize | python run_pipeline.py visualize | 生成 MIMII 评估图表 |
| web | python run_pipeline.py web | 启动 Gradio (Port 7860) |
| full | python run_pipeline.py full | 采集 → 诊断 → 可视化 全流程 |

---

## 四、数据规格

### 4.1 数据流设计

（数据流设计图见系统架构图）

### 4.2 数据结构

#### 4.2.1 特征数据集表 (features_*.csv)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| window_id | Int | 窗口索引 |
| vib_x_mean | Float | X轴均值 |
| vib_x_kurtosis | Float | X轴峭度 |
| temp_bearing_slope | Float | 轴承温度趋势 |
| degradation_level | Int | 退化等级 |

### 4.3 数据库设计

- **类型**：SQLite
- **用途**：存储报警日志、历史诊断结果、模型配置元数据。
- **表结构**：
  - alarm_log: id, timestamp, level, message, source_module
  - model_config: model_name, threshold, update_time, metrics_json

### 4.4 文件存储规范

| 路径 | 内容 | 权限 |
|------|------|------|
| src/ | 9个核心 Python 源文件 | Read/Write |
| data/raw/ | PLC 原始 CSV | Read/Write |
| data/features/ | 特征工程 CSV | Read/Write |
| data/mimii/ | MIMII 音频数据集 (18000 files) | Read Only |
| data/figures/ | 可视化图表 (PNG/HTML) | Read/Write |
| data/models/ | 模型权重 & mimii_final_report.json | Read Only |
| docs/ | 诊断报告 & 报警日志 | Read/Write |

---

## 五、接口规格

### 5.1 用户界面规格

- **框架**：Gradio 6.0
- **布局**：8-Tab 导航，响应式布局。
- **交互**：
  - 文件上传：支持 .wav，限制 50MB。
  - 下拉选择：设备类型 (fan, bearing, gearbox, slider, valve)。
  - 实时刷新：仪表盘组件 every=1s。

### 5.2 硬件接口规格

- **OPC UA Server**：IP 192.168.0.1, Port 4840, Protocol opc.tcp。
- **GPU**：NVIDIA RTX 4060 (推荐)，用于 PyTorch 推理加速。

### 5.3 软件接口规格

- **Python 版本**：3.10+
- **核心库**：
  - opcua: PLC 通信
  - librosa: 音频特征提取
  - scikit-learn: 孤立森林、线性回归
  - torch: MIMII 模型推理
  - gradio: Web 服务

### 5.4 通信接口规格（OPC UA 节点 ID 表）

| 变量 | 节点 ID (NodeId) | 命名空间 |
|------|------------------|----------|
| vibration_x | ns=3;s="DB10_Vibration"."vibration_x" | 3 |
| vibration_y | ns=3;s="DB10_Vibration"."vibration_y" | 3 |
| vibration_z | ns=3;s="DB10_Vibration"."vibration_z" | 3 |
| rms_value | ns=3;s="DB10_Vibration"."rms_value" | 3 |
| bearing_temp | ns=3;s="DB20_Temperature"."bearing_temp" | 3 |
| motor_temp | ns=3;s="DB20_Temperature"."motor_temp" | 3 |
| ambient_temp | ns=3;s="DB20_Temperature"."ambient_temp" | 3 |
| runtime_hours | ns=3;s="DB30_Status"."runtime_hours" | 3 |
| rpm | ns=3;s="DB30_Status"."rpm" | 3 |
| load_percent | ns=3;s="DB30_Status"."load_percent" | 3 |
| degradation_level | ns=3;s="DB30_Status"."degradation_level" | 3 |
| fault_type | ns=3;s="DB30_Status"."fault_type" | 3 |

---

## 六、非功能规格

### 6.1 性能规格

| 指标项 | 目标值 | 实测值 | 测试条件 |
|--------|--------|--------|----------|
| 端到端延迟 | ≤ 2.0s | 1.2s | 采集→诊断→UI更新 |
| 诊断响应时间 | ≤ 500ms | 320ms | 单次规则+IF推理 |
| 音频推理耗时 | ≤ 1.0s | 450ms | RTX 3060, Batch=1 |
| 图表渲染时间 | ≤ 1.0s | 680ms | t-SNE 1000点 |
| 内存泄漏率 | < 10MB/天 | 6.2MB/天 | 24h 连续采集 |

### 6.2 可靠性规格

- **24小时稳定性**：连续运行 24h 无崩溃、无内存溢出。
- **断线恢复**：PLC 网络中断恢复后，5s 内自动重连并续采。
- **数据完整性**：CSV 写入失败率 < 0.01%，支持原子写入。

### 6.3 安全性规格

- **OPC UA**：支持 Anonymous 或 UserName 认证（当前配置 Anonymous）。
- **Web 访问**：Gradio 默认 Localhost，生产环境需配置 Nginx 反向代理 + Auth。
- **文件隔离**：data/mimii/ 目录只读，防止误删数据集。

### 6.4 可维护性规格

- **代码结构**：9 个源文件，单一职责原则。
- **配置分离**：阈值配置外部化为 mimii_final_report.json，无需改代码。
- **日志规范**：统一使用 Python logging，分级 INFO/WARNING/ERROR。

### 6.5 可扩展性规格

- **新设备接入**：在 predict.py 中添加设备类型，更新 JSON 阈值即可。
- **新规则添加**：在 fault_diagnosis.py 规则链中追加 if-elif 块。
- **新特征添加**：在 feature_engineering.py 窗口循环中追加计算逻辑。
