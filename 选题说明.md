## 一、选题说明

### 1.1 拟定题目

本课题拟定题目为：**基于博图PLC与深度学习的设备预测性维护与实时故障检测系统**。

### 1.2 完成目标

本课题旨在构建一套完整的"控制层—分析层—展示层"三层架构的工业预测性维护系统，具体完成以下四个层次的目标：

1. **数据采集层**：基于 TIA Portal（博图）仿真环境，利用 S7-1200/1500 PLC 模拟工业设备运行，通过 OPC UA 协议实时采集振动、温度等多维传感器数据，打通"控制层"与"分析层"的数据链路。
2. **智能分析层**：基于 Python 深度学习框架（PyTorch/TensorFlow），构建 CNN、LSTM、Autoencoder 三类核心模型，分别实现故障分类、剩余使用寿命（RUL）预测和异常检测三大运维场景。
3. **系统集成层**：采用 FastAPI 搭建后端推理服务，InfluxDB 存储时序数据，MySQL 存储设备台账与报警记录，Vue + ECharts 构建 B/S 架构的 Web 监控大屏，实现从数据采集、实时推理到可视化展示的完整闭环。
4. **工程化落地**：完成模型轻量化（剪枝与量化），探索边缘计算部署方案，利用 AI 辅助编程（Vibe Coding）提升开发效率，并通过功能测试与压力测试验证系统稳定性。

### 1.3 涉及的技术方向

本课题深度融合了上学期多门核心课程的技术专题，具体应用情况如下表所示：

| 序号 | 技术方向 | 涉及的上学期课程专题 | 在本课题中的应用 |
|------|----------|----------------------|------------------|
| 1 | 工业通信协议与物联网 | 工业以太网、OPC UA 通信协议、PLC 编程 | 在博图 PLC 中启用 OPC UA 服务器，配置用户权限与安全策略，实现传感器变量的标准化发布；通过 PLCSIM Advanced 仿真设备运行，生成包含正常与故障特征的仿真数据。 |
| 2 | 深度学习与人工智能 | 卷积神经网络（CNN）、循环神经网络（LSTM/GRU）、自编码器（Autoencoder） | 利用 CNN 对振动信号经 FFT 变换后的频谱图进行轴承故障分类；利用 LSTM/GRU 输入多变量时序数据预测设备 RUL；利用 Autoencoder 无监督学习实现早期异常检测。 |
| 3 | 软件工程与系统架构 | RESTful API 设计、前后端分离架构、数据库设计、版本控制（Git） | 使用 FastAPI 开发后端 RESTful API，集成 OPC UA 客户端实现实时数据订阅；使用 Vue + ECharts 开发前端监控大屏；使用 InfluxDB + MySQL 构建混合数据存储架构；使用 Git 进行项目版本管理。 |
| 4 | 工业大数据分析 | 时序数据处理、特征工程、数据清洗与归一化 | 使用 Pandas/Numpy 进行数据清洗（3σ 原则去噪、插值填充），采用滑动窗口（Sliding Window）处理与时序归一化，将原始传感器数据转化为模型可用的训练样本。 |
