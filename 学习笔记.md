# 基于博图PLC的深度学习预测性维护与实时故障检测

**工业通信协议**：
**OPC UA 配置**：在博图中启用OPC UA服务器，配置用户权限与安全策略，实现变量的标准化发布。这是连接PLC与Python/AI系统的首选桥梁。

## 一、使用工具的学习

**仿真环境搭建**：

- 使用 **PLCSIM Advanced** 模拟设备运行，编写脚本生成包含“正常”与“故障”特征的仿真数据（如加入噪声、趋势项）。
  **实战任务**：- 在博图中编写程序，采集模拟的振动、温度数据，并通过OPC UA在本地电脑上成功读取到这些实时数值。
  **工具选择**：
  任务书列的候选工具有 ZCode、Trae、Qwen Code、通义灵码、Deepseek Harness、Claude Code、Codex、Open Code，我挑了几个了解了一下：

- **Trae**：是个独立的 IDE，功能挺全，但我 VSCode 已经用习惯了，不想再换一个软件重新适应；
- **Claude Code / Codex**：网上评价很好，但要折腾网络还要付费订阅，学生党劝退；
- **通义灵码 / Qwen Code**：也是 VSCode 插件，国内用着方便，留作备选；
- **Kimi Code**：VSCode 插件，装完就能用，配的是 K3 模型。我平时用 Kimi 查资料比较多，中文交流顺畅，长上下文也适合"让它先读整个项目再改"的用法。

综合考虑：**主力用 Kimi Code + K3，如果中途不好用就换通义灵码或 Qwen Code。**

## 二、深度学习核心算法与模型构建

**目标**：掌握制造智能的“大脑”，针对不同的运维场景选择合适的神经网络模型。

- **核心学习内容**
  - **数据预处理（Python栈）**：
    - 使用 Pandas/Numpy 进行数据清洗（3σ原则去噪、插值填充）。
    - **关键技能**：时序数据的**滑动窗口（Sliding Window）**处理与 Min-Max 归一化，这是将原始数据转化为模型输入的关键。
  - **三大核心算法场景**：
    - **场景A：故障分类（CNN）**
      - 原理：利用卷积核提取特征。
      - 应用：将振动信号通过 **FFT（快速傅里叶变换）** 转为频谱图，输入CNN识别轴承内圈/外圈故障。
    - **场景B：寿命预测（LSTM/GRU）**
      - 原理：利用门控机制解决长序列梯度消失。
      - 应用：输入多变量时序数据，预测设备的 **RUL（剩余使用寿命）**。
    - **场景C：异常检测（Autoencoder）**
      - 原理：无监督学习，基于重构误差判断异常。
      - 应用：在故障样本极少的情况下，仅用正常数据训练，识别早期异常。
- **实战任务**
  - 下载 **CWRU（凯斯西储大学）轴承数据集** 或 **NASA C-MAPSS 数据集**。
  - 使用 PyTorch/TensorFlow 搭建一个 LSTM 模型，实现对设备剩余寿命的预测，并绘制出预测曲线。

## 三、IT/OT 融合与系统架构开发（预计耗时：3-4周）

**目标**：打通“控制层”与“分析层”，构建符合B/S架构的完整制造智能系统。

- **核心学习内容**
  - **后端服务开发**：
    - 使用 Python (FastAPI/Flask) 开发 RESTful API。
    - 集成 **OPC UA 客户端**（如 asyncua 库），实现从PLC订阅实时数据。
    - 编写**推理服务**：加载训练好的模型，对实时流入的数据进行在线预测。
  - **数据存储架构**：
    - **时序数据库**：学习使用 **InfluxDB** 存储高频传感器数据（PLC -> InfluxDB）。
    - **关系型数据库**：使用 MySQL 存储设备台账、报警记录和用户信息。
  - **前端可视化（Web端）**：
    - 使用 Vue + ECharts 开发监控大屏。
    - 展示内容：实时参数曲线、故障报警弹窗、RUL 预测趋势图。
- **实战任务**
  - 搭建一个闭环系统：PLC产生数据 -> OPC UA传输 -> Python后端接收并推理 -> 判定故障 -> 写回PLC触发报警标志位。

## 四、工程化落地与Vibe Coding实践（预计耗时：2周）

**目标**：提升系统的稳定性、实时性，并利用AI辅助编程提高效率。

- **核心学习内容**
  - **模型部署与优化**：
    - **模型轻量化**：学习模型剪枝与量化，降低推理延迟（目标 <100ms）。
    - **边缘计算**：了解如何将模型部署在边缘网关，而非云端，以保证实时性。
  - **Vibe Coding（AI辅助开发）**：
    - **PLC代码生成**：使用提示词让AI生成结构化的梯形图或SCL代码（如：“生成S7-1200的OPC UA数据读取块”）。
    - **算法代码生成**：利用AI快速搭建深度学习模型框架，通过明确输入输出格式（如：“输入50步长时序数据，输出RUL值”）来验收代码。
  - **系统测试**：
    - 功能测试：验证故障检测准确率。
    - 压力测试：验证高并发数据下的系统延迟。

---

### 学习路线关键映射表

| 学习阶段             | 涉及工具/技术                              | 对应制造智能核心能力                                        |
| -------------------- | ------------------------------------------ | ----------------------------------------------------------- |
| **阶段一：数据获取** | TIA Portal, S7-1200/1500, OPC UA, PLCSIM   | **工业物联网感知能力**：解决“数据孤岛”，实现设备互联。      |
| **阶段二：智能分析** | Python, PyTorch/TF, LSTM, CNN, Autoencoder | **工业大数据分析能力**：从数据中挖掘价值，实现预测性维护。  |
| **阶段三：系统集成** | FastAPI, InfluxDB, Vue, ECharts, Docker    | **全栈系统架构能力**：构建B/S架构的工业APP，实现IT/OT融合。 |
| **阶段四：工程实践** | Git, ONNX, 边缘计算, Prompt Engineering    | **工程落地与创新能力**：解决实际部署中的延迟、稳定性问题。  |

## 五、Git 的原理和功能

版本控制工具，记录代码的每一次修改，改错了能退回去。是"分布式"的，意思是每个人电脑上都存着完整的修改历史，断网也能用。主要记住三个区域：

- **工作区**：我正在改的文件；
- **暂存区**：add 之后相当于放进"购物车"，可以先攒着；
- **本地仓库**：commit 之后才算真正存了一个版本；
- **push** 之后 GitHub 上才能看到。

### 常用命令（实操中记的）

| 命令                   | 作用                                       |
| ---------------------- | ------------------------------------------ |
| `git clone 网址`       | 把仓库下载到电脑                           |
| `git add 文件名`       | 放进暂存区                                 |
| `git commit -m "说明"` | 提交一个版本，说明要写清楚                 |
| `git push origin main` | 传到 GitHub                                |
| `git status`           | 看看现在什么状态（不知道怎么办就先敲这个） |
| `git log`              | 看提交历史                                 |

补充两个名词：`origin` 就是 GitHub 上那个仓库的代号（clone 的时候自动设好的），`main` 是主分支的名字。我一开始以为 origin 是个要单独配置的东西，折腾了半天。直到我试了 `git remote -v` 才 realizing。

## 六、学习建议

1. **不要试图一次性完美**：先用PLCSIM仿真数据跑通全流程，再上真实硬件。
2. **重视数据质量**：在制造智能中，垃圾数据进，垃圾结果出（GIGO）。博图中的采样周期设置和滤波处理非常关键。
3. **利用AI提效**：你提到的“Vibe Coding”非常实用。在写博图SCL代码或Python数据清洗脚本时，多让AI生成模板，你负责核心逻辑的审查与修改。

4. **多尝试**：多尝试不同的模型，多尝试不同的数据集，多尝试不同的数据处理方法。
